Небольшой пет-проект на Dart для парсинга цен с определенных сайтов. 

# короткий режим (по умолчанию)
dart run bin/main.dart

# полный DEBUG через compile-time
dart run -DDEBUG=true bin/main.dart

# полный DEBUG через environment variable
DEBUG=true dart run bin/main.dart